apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: esprit-formation
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: user-service
          image: oumaimakachai/user-service:latest
          env:
            - name: EUREKA_SERVER_URL
              value: http://eureka-server:8761/eureka
            - name: SPRING_DATASOURCE_URL
              value: jdbc:mysql://mysql:3306/formationdb
            - name: SPRING_DATASOURCE_USERNAME
              value: root
            - name: SPRING_DATASOURCE_PASSWORD
              value: root
          ports:
            - containerPort: 8081
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: esprit-formation
spec:
  selector:
    app: user-service
  ports:
    - port: 8081
      targetPort: 8081
